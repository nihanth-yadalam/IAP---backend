[pytest]
markers =
    integration: Integration tests (multi-step API workflows)
    regression: Regression tests (edge cases and bug prevention)
    e2e: End-to-end tests against live infrastructure
asyncio_mode = auto
